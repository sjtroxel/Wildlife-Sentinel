{
  "name": "@wildlife-sentinel/scripts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "ingest:species": "npm run build && node --env-file=../server/.env dist/ingestSpeciesFacts.js",
    "ingest:conservation": "npm run build && node --env-file=../server/.env dist/ingestConservationContext.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "postgres": "^3.4.4",
    "shapefile": "^0.6.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  }
}
